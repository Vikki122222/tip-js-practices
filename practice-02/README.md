# Отчёт по практической работе № 2

**Тема:** Функции, объекты, массивы и организация кода

## 1. Автор и окружение

**Имя:** Виктория  
**Группа:** ЭФБО-06-25  
**Вариант:** 3  
**Операционная система:** Windows 11  
**Node.js:** фактический вывод `v24.21.0`  
**npm:** фактический вывод `11.19.0`

## 2. Запуск

Из корня личного репозитория:

```bash
node practice-02/examples/functions-and-references.js
node practice-02/src/main.js
node practice-02/checks.js
```

Альтернатива — из папки `practice-02`:

```bash
npm run examples
npm start
npm run check
```

Зависимостей нет; `npm install` не требуется.

## 3. Функции, ссылки и копирование

В первой части работы были рассмотрены параметры функций, возвращаемые значения, стрелочные функции, ссылки на объекты, копирование массивов и объектов, а также параметры по умолчанию.

| № | Прогноз до запуска | Фактический результат | Причина |
|---:|---|---|---|
| 1 | `5`, затем `23` | `5`, затем `23` | При сложении числа и строки оператор `+` выполняет склейку текста, а не обычное сложение чисел. |
| 2 | `16` | `16` | В стрелочной функции с фигурными скобками результат нужно явно вернуть через `return`. После исправления функция возвращает `16`. |
| 3 | `5`, затем `true` | `5`, затем `true` | При присваивании объекта другой переменной передаётся ссылка на тот же объект. Поэтому изменение через `alias` изменяет и `product`. |
| 4 | `7`, `false`, `true` | `7`, `false`, `true` | Оператор `...` создаёт новый массив, но объекты внутри него остаются общими. Поэтому изменение объекта через `copy` видно и в исходном массиве. |
| 5 | `3 8 3`, `false` | `3 8 3`, `false` | При копировании объектов одинаковое свойство перезаписывается следующим значением. Поэтому важен порядок записи свойств. |
| 6 | `Без названия`, `Без названия`, `null` | `Без названия`, `Без названия`, `null` | Значение параметра по умолчанию используется, если аргумент не передан или передано `undefined`. Значение `null` не заменяется значением по умолчанию. |

### Как был исправлен второй эксперимент

Изначально функция `square` не возвращала результат, так как внутри фигурных скобок не было `return`.

После исправления:

```js
const square = (value) => {
  return value * value;
};
```

функция стала возвращать правильный результат:

```text
16
```

---

## 4. Модель данных и функции

Для работы с задачами была создана модель со следующими полями:

- `id` - уникальный идентификатор задачи;
- `title` - название задачи;
- `completed` - состояние выполнения;
- `priority` - приоритет задачи.

Основная логика работы с задачами находится в файле `src/task-service.js`.

В файле реализованы следующие функции:

- `createTask` - создание задачи;
- `findTaskById` - поиск задачи по идентификатору;
- `getPendingTasks` - получение невыполненных задач;
- `getTaskTitles` - получение названий задач;
- `getTaskStats` - получение статистики;
- `addTask` - добавление задачи;
- `setTaskCompleted` - изменение состояния выполнения;
- `renameTask` - изменение названия;
- `removeTask` - удаление задачи.

Успешные операции возвращают результат с `ok: true`, а при ошибке возвращается `ok: false` и описание ошибки.

### Почему `id` не равен индексу

Индекс - это позиция элемента в массиве. Она может измениться после удаления или добавления других задач.

`id` - это идентификатор конкретной задачи, который остаётся постоянным. Поэтому для поиска и изменения задач используется именно `id`.

### Почему `getTaskStats` возвращает число

Функция `getTaskStats` отвечает только за вычисление статистики. Она возвращает числовое значение `progress`.

Преобразование числа в строку, округление и добавление символа `%` выполняется уже при выводе результата.

### Какие объекты копируются при изменении задачи

При изменении задачи создаётся новый массив и новый объект только для изменяемой задачи.

Остальные задачи остаются без изменений. Благодаря этому исходный массив не изменяется напрямую.

### Собственные проверки

Помимо обязательных проверок из задания были добавлены три собственных сценария.

| Проверка | Входные данные | Ожидаемый результат | Фактический результат | Итог |
|---|---|---|---|---|
| Добавление после удаления | Сначала удалить задачу с `id = 4`, затем добавить новую задачу с `id = 4` | Новая задача добавляется в конец массива, исходный массив не изменяется | Новая задача добавлена в конец, исходный массив не изменился | Пройдено |
| Изменение первой и последней задачи | Изменить `completed` у задачи `id = 1` и название задачи `id = 10` | Оба изменения выполняются, остальные поля сохраняются | Оба изменения выполнены, остальные поля сохранились | Пройдено |
| Несколько изменений подряд | Выполнить задачу `id = 50`, затем переименовать задачу `id = 60` | Оба изменения применяются последовательно, исходный массив не изменяется | Оба изменения выполнены, исходный массив не изменился | Пройдено |

### Результат проверок

Всего выполнено **38 проверок**:

- 35 обязательных проверок;
- 3 собственные проверки.

Результат:

```text
Проверок пройдено: 38; не пройдено: 0.
```

---

## 5. Общий демонстрационный сценарий

В общем сценарии использовался исходный массив `demoTasks`.

Начальное состояние:

| Состояние | Всего | Выполнено | Осталось | Прогресс |
|---|---:|---:|---:|---:|
| Исходный набор | 4 | 2 | 2 | 50.0% |
| Добавление `id = 20` | 5 | 2 | 3 | 40.0% |
| Выполнение `id = 4` | 5 | 3 | 2 | 60.0% |
| Переименование `id = 10` | 5 | 3 | 2 | 60.0% |
| Удаление `id = 7` | 4 | 3 | 1 | 75.0% |

В начале сценария были выведены:

- исходные задачи;
- названия задач;
- список невыполненных задач;
- начальная статистика.

После этого последовательно были выполнены следующие действия:

1. Добавлена задача с `id = 20` и высоким приоритетом.
2. Задача с `id = 4` отмечена как выполненная.
3. У задачи с `id = 10` изменено название на `Подготовить инструкцию запуска`.
4. Удалена задача с `id = 7`.
5. Выполнена попытка повторно добавить задачу с `id = 20`.

### Итоговый список идентификаторов

После выполнения всех операций в массиве остались задачи с идентификаторами:

```text
1, 4, 10, 20
```

### Проверка добавления дубликата

При попытке добавить задачу с уже занятым `id = 20` программа вернула ошибку:

```text
ID уже существует
```

Исходный список при этом не изменился.

### Подтверждение неизменности `demoTasks`

После выполнения всех операций `currentTasks` содержит 4 задачи, а исходный `demoTasks` также содержит 4 исходные задачи.

Данные в `demoTasks` не изменились.

---

## 6. Проверки

Для запуска автоматических проверок использовалась команда:

```bash
npm run check
```

Фактический результат:

```text
OK: 01. Создание задачи с явным приоритетом
OK: 02. Приоритет по умолчанию
OK: 03. Удаление краевых пробелов без изменения внутренних
OK: 04. Граничные длины названия и положительный безопасный id
OK: 05. Некорректные названия при создании
OK: 06. Некорректные идентификаторы при создании
OK: 07. Все допустимые приоритеты и отклонение недопустимых
OK: 08. Каждый вызов createTask возвращает новый объект
OK: 09. Поиск по id, а не по индексу
OK: 10. Отсутствующая задача и пустой массив при поиске
OK: 11. Поиск не преобразует строковый id в число
OK: 12. Получение невыполненных задач
OK: 13. Пустой список, все выполнены и все не выполнены
OK: 14. Названия в исходном порядке и пустой список
OK: 15. Сводка по общему набору
OK: 16. Сводка по пустому списку
OK: 17. Сводка: ничего не выполнено и выполнено всё
OK: 18. Процент остаётся числом без предварительного округления
OK: 19. Добавление в конец без изменения входного массива
OK: 20. Добавление в пустой список с приоритетом по умолчанию
OK: 21. Повторяющийся id отклоняется
OK: 22. Добавление не обходит проверки createTask
OK: 23. Изменение completed в обе стороны
OK: 24. completed принимает только boolean
OK: 25. Некорректный или отсутствующий id при изменении статуса
OK: 26. Повторная установка статуса создаёт новый результат
OK: 27. Переименование сохраняет остальные поля
OK: 28. Проверки названия при переименовании
OK: 29. Некорректный или отсутствующий id при переименовании
OK: 30. Удаление по id с сохранением порядка
OK: 31. Удаление единственной записи
OK: 32. Некорректный, отсутствующий id и удаление из пустого списка
OK: 33. Входные объекты не изменяются ни одной операцией
OK: 34. Общий сценарий и все промежуточные сводки
OK: 35. Работа с другим набором, без зависимости от demoTasks
OK: Собственный случай: добавление после удаления
OK: Собственный случай: изменение первой и последней задачи
OK: Собственный случай: несколько изменений подряд

Проверок пройдено: 38; не пройдено: 0.
```

**Дата запуска:** 19 сентября 2026 г.  


---

## 7. Индивидуальный вариант

**Вариант:** 3

**Тема:** создание сайта-портфолио.

Исходный набор содержит 6 задач. Первые две задачи выполнены, остальные четыре - нет.

Идентификаторы исходных задач:

```text
11, 23, 37, 41, 58, 64
```

Начальное состояние:

| Состояние | Всего | Выполнено | Осталось | Прогресс |
|---|---:|---:|---:|---:|
| Начальный набор | 6 | 2 | 4 | 33.3% |
| Добавление `id = 80` | 7 | 2 | 5 | 28.6% |
| Выполнение `id = 11` | 7 | 2 | 5 | 28.6% |
| Переименование `id = 23` | 7 | 2 | 5 | 28.6% |
| Удаление `id = 37` | 6 | 2 | 4 | 33.3% |

В индивидуальном сценарии были выполнены следующие действия:

1. Добавлена задача с `id = 80` и приоритетом `low`.
2. Для задачи `id = 11` установлено состояние `completed = true`.
3. Задача с `id = 23` переименована в `Выбрать итоговый цветовой профиль`.
4. Удалена задача с `id = 37`.
5. Выполнена попытка повторно добавить задачу с `id = 80`.

При повторном добавлении задачи с `id = 80` программа вернула ошибку:

```text
ID уже существует
```

Итоговый список содержит следующие идентификаторы:

```text
11, 23, 41, 58, 64, 80
```

Итоговая статистика:

- всего задач - 6;
- выполнено - 2;
- осталось - 4;
- прогресс - 33.3%.

---

## 8. Расширение

Расширение задания не выполнялось.

---

## 9. Вывод

В практической работе я использовала функции, объекты и массивы, а также разделила данные и основную логику по отдельным файлам.

Для работы с задачами были реализованы функции создания, поиска, добавления, изменения и удаления.

Отдельно была проверена работа с исходными массивами. При выполнении операций создавались новые массивы и объекты, поэтому исходные данные не изменялись.

Также были рассмотрены параметры функций, стрелочные функции, ссылки на объекты, копирование массивов и объектов, порядок свойств и параметры по умолчанию.

Во втором эксперименте была исправлена стрелочная функция: при использовании фигурных скобок результат необходимо явно возвращать с помощью `return`.

В результате все обязательные и собственные проверки были успешно пройдены.

---

## 10. Самопроверка

- [x] Все 9 обязательных функций реализованы.
- [x] Общий демонстрационный сценарий выполнен.
- [x] Индивидуальный вариант выполнен.
- [x] Добавлены 3 собственные проверки.
- [x] Все 35 обязательных проверок пройдены.
- [x] Все 3 собственные проверки пройдены.
- [x] Проверено отсутствие изменения исходных массивов.
- [x] Фактические результаты запуска отражены в отчёте.
- [x] Инструкции по запуску проверены.
- [x] Код и отчёт отправлены в существующий учебный репозиторий.